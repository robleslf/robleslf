name: Generate Trophies

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download Trophies
        run: |
          # Usamos 'darkhub' que es compatible y se ve genial
          curl -L "https://github-profile-trophy.vercel.app/?username=${{ github.repository_owner }}&theme=darkhub&column=7" -o github-trophies.svg

      - name: Commit and Push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update trophies with valid theme"
          file_pattern: 'github-trophies.svg'
